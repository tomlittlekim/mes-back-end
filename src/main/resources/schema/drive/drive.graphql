type FileManagement {
    id: Int
    name: String
    extension: String,
    path: String,
    size: Int
    menuId: String
}

extend type Mutation {
    deleteFile(id: Int): String
}

extend type Query {
    getAllFiles: [FileManagement]
}